{% for type, message in app.flashes(['success', 'danger', 'info']) %}
    {% for message in message %}
        <div class="col-md-12">
            <div class="alert alert-{{ type }} alert-dismissible fade show" role="alert">
                {{ message }}
            </div>
        </div>
    {% endfor %}
{% endfor %}